# CLAUDE-HANDOFF — session continuity log

_Give this file to a fresh Claude session to continue exactly from here. Keep it updated at every meaningful step (the operator has asked for this explicitly). Last update: **2026-07-10 ~16:05 UTC**, branch `claude/supervisor-state-recovery-4mp2vg` (PR #139 merging)._

## 0-TODAY (2026-07-10): harness → fixes → overhaul arc
- **Merged today**: PR #136 (conversation intake + scheduler + Campaign Intelligence + 3 builder integrations: appointments import, competitor intel, objection taxonomy), #137 (social ready-queue + Meta credential gate + cited lead enrichment + conversation identity resolution), #138 (harness-defect fixes: anti-fabrication live-ops injection into chat+voice via `studio.inventory.live_operations_block`, artifacts ensure_schema once-per-process, Meta gate exempts comment replies, b-roll + ready-queue tenant scoping, Campaign Intelligence tenant scoping). **PR #139 in flight** (engine truth-gaps: real run durations from step spans, `stepsTotal`/`draftsStaged`, async `run_campaign` launch, in-flight fleet union, cohort-conformance note + artist sign-off gate, S2 objection-family guard; console UX overhaul: plain language everywhere, dates on all timestamps, URL-hash routing w/ back-forward, StatusStrip, motion; tenant-mismatch guard: `/healthz.studioTenant` + boot line + console warning strip; lineage "no recorded evidence" alert).
- **Data**: 31 verbatim Keebs conversation threads (439 turns) ingested (`keebs-conversations-v2.csv`, delivered to operator); identities merged onto real-email rows by phone (+name disambiguation for shared phones — Kassie/Jessica case); opt-outs auto-flagged (Kassie, Jaclyn, Amanda Kuhl); 123 real appointments/$146,688 deposits in; ALL QA harness debris purged from skindesign (customers/artifacts/assets/memories/chat sessions).
- **Harness ran** (operator-ordered): 17 internal scenarios graded REAL/FAKE vs SQL + 6 browser personas + diff review (~45 agents). FAKEs found & fixed: host lied about queue (now injected truth — retest answers exact DB count), executive-brain cross-tenant leak (skindesign's "93 price leads" was QA-fixture-inflated; true=5), b-roll leak, artifacts deadlock. Scorecard: 13 PASS / 2 PARTIAL / 2 FAIL (both FAILs fixed). Self-test playbook delivered to operator.
- **Operator's own-machine finding**: engine started by hand w/o STUDIO_TENANT_ID served 'demo' → Artists "0", artworks "orphaned". Root-caused (silent `demo` fallback in `library_api._tenant`); guarded 3 ways (healthz field + boot warning + console mismatch strip). Their fix: start engine via `scripts\run-local.ps1`.
- **Still open**: Meta page token (only blocker for real IG/FB publish + Keebs own-posts pull — everything staged/gated to that boundary); competitor reels export from operator; gold-set hand-labeling of the 31 objection classifications; supervised one-command bring-up; feedback/outcome loop (sends → replies/bookings back into campaign memory); PR #107 RLS. Unknown string from operator (`i2hD…WZ4I`) — asked which system it belongs to, no answer yet.
- **Ops crib**: PG dies randomly → `pg_ctlcluster 16 main start`. pkill exits 144 — restart in a separate command. Engine start needs explicit `ENGINE_DATABASE_URL` + `STUDIO_TENANT_ID=skindesign` (boot line confirms tenant). Console :3000 `npm run dev`. Merge protocol: green → un-draft → merge → reset branch on main → prune stale tracking ref → push.

## 0. TL;DR
The whole product now runs from a fresh clone: `scripts/run-local.sh` (PG → initdb → `engine/bootstrap_db.py` → engine :8000 → console :3000). Real skindesign data is imported (1,092 customers, 37 artists, 5 real past campaigns as campaign memory + substantiated offers). Draft-count exactness, channel routing (email vs Instagram crews), VLM artwork upload → artist memory, top-4 artwork mid-run pause → operator choice → resume, live agent panel, research safety (protected-traits ban), delivery hardening (attachments, SMTP fallback, per-action IG media) are all built and were verified LIVE in this session. Delivery of real email is blocked only on credentials/egress (see `docs/CREDENTIALS-AND-EXTERNALS.md`, statuses verified). PR #124 (draft) carries everything; CI is being driven green; operator has ordered: **merge all PRs once green**.

## 1. Where things run (this cloud session)
- Postgres 16 **native** (not docker; docker registry blocked by egress policy): `postgresql://scalers:scalers@localhost:5432/scalers`, pgvector 0.6.0.
- Redis native :6379. MinIO NOT running (uploads store to disk under `engine/var/artifacts/` instead — by design of the new upload path).
- Engine: `cd engine && ENGINE_DATABASE_URL=postgresql://scalers:scalers@localhost:5432/scalers SCALERS_EMBEDDER=deterministic STUDIO_TENANT_ID=skindesign uv run uvicorn main:app --host 127.0.0.1 --port 8000`
- Console: `cd web && npm run dev -- --port 3000` (`.env.local` → tenant skindesign, backend :8000).
- Secrets: `engine/.env` (gitignored, chmod 600). Contains ANTHROPIC ✅, FIRECRAWL ✅, META (page perms missing), GMAIL client + DEAD refresh token, OPENAI ✅ (added 07-09), SMTP_SENDER/SMTP_APP_PASSWORD ✅ (added 07-09), GMAIL_REDIRECT_TO=operator inbox.

## 2. What was done this session (all verified live, not claimed)
1. **Fresh-clone fixes**: committed missing DDL (`infra/initdb/15-customers.sql`, `21-customer-personas.sql` — customers/customer_personas/tattoo_history existed only on the old dev machine); `engine/bootstrap_db.py` provisions all ~20 lazily-created store tables; `scripts/run-local.sh` one-shot bring-up.
2. **Real data imported** via `python -m studio.client_import .inbound skindesign` (1,092 customers, 37 artists; tenant forced test_mode=TRUE). PII stays in gitignored `.inbound/`.
3. **Campaign memory**: 5 real past campaigns (Christina $2,800 / Kirshten / Natalie $1,200 / Keebs $1,200 / Lynn) imported into `campaign_examples` (+10 extracted patterns) from operator screenshots → `.inbound/campaign-examples.json`.
4. **Real offers doc** uploaded (kind=offers): codes KEEBS ($1,200 full-day), CHRISTINA ($2,800), PAYPLAN (Klarna/Affirm) — drafts may now cite these; anything else is blocked by the substantiation gate.
5. **Draft-count exactness (spec §14)**: two root-cause fixes — (a) warm/past-customer framing now gates on real relationship evidence (was: profile-presence ⇒ every cold lead got "welcome back" copy ⇒ guard killed it ⇒ 3 asked/0 staged); (b) guard-killed drafts refill from the next contactable customer (bounded 2×, cohort path only). Live: 3→3, 5→5 with the real KEEBS offer.
6. **Landed 9 stranded origin branches**: nmh.7 (research agent), nmh.6 (dossier/campaign memory), nmh.9 (**channel routing** — conflicts resolved), nmh.11 (staging stability), tlv.6 (demo slice), bug/4hj, 65w14, 4jx13 (server-side hold), wwy.5.
7. **4 parallel builder agents** (worktrees, all merged):
   - ENGINE-CORE: image upload → disk (`var/artifacts/`) + VLM (citation-gated `ingest_vlm`, now actually wired) + artist link + artist memory row; read APIs `GET /studio/artists`, `/studio/artists/{slug}`, `/studio/artifacts`(+`/raw`), `POST /studio/artists/{slug}/memory`; artwork top-4 + `awaiting_selection` pause + `POST /studio/campaign/{run_id}/select-artwork` durable resume; supervisor live-state tools (chat host + voice `liveState`); stage_publish run_id lineage; IG pipeline depth (artist_memory + trend_research crew steps, Firecrawl-cited).
   - WEB-CONSOLE: Artists tab (roster/profile/gallery/upload+prompt/past campaigns/memory timeline), role-driven LIVE agent panel (no hardcoded pipeline; shows exactly the roles that ran), artwork picker modal, Runs tab states, 10-defect QA sweep, artifact library on Campaign memory.
   - RESEARCH-SAFETY: deterministic protected-traits ban (9 categories, prompt + post-filter + `trait_filtered` audit) across psych/research/social; consumer-shaped research queries; `research_depth=="deep"` gate; consent-safe social (customer-provided handles only).
   - DELIVERY: Gmail attachments (fail-closed), **SMTP fallback** (engages only on dead Gmail token, below the whole gate stack), per-action IG media + `PUBLIC_ASSET_BASE_URL`, `SCALERS_OUTPUT_HARD_CAP` (1..500), unified send_audit rows.
8. **E2E scenario verified live**: IG run = artist_memory→trend_research→researcher→strategist→draft→critic→jury crew, paused for artwork, resumed after choice; review queue 7 HELD items newest-first (2 IG posts + 5 emails, real recipients); Keebs profile page shows real past campaign (152 delivered/32 failed/50 DND) + 3 uploaded artworks + memory timeline; TEST-MODE server gate REFUSED a real-customer approve; allowlisted operator-inbox approve exercised Gmail(dead)→SMTP fallback→honest network error (this sandbox blocks TCP:465 — works on a normal machine).

## 3. Known truths / gotchas
- **Anthropic + Firecrawl work through this sandbox's proxy; SMTP (raw TCP), Google Drive, huggingface.co are blocked** by the environment egress policy.
- Gmail OAuth refresh token is EXPIRED/REVOKED (verified via token exchange). Re-consent (Option A) or run on a machine where SMTP egress works (Option B creds already in `.env`).
- Meta page token lacks page permissions (OAuthException 190/145) — see credentials doc §5.
- Engine model policy clamp (haiku default / sonnet-4-5 ceiling) is ACTIVE and must stay (CustomerAcq-8sk).
- Safety spine intact and re-verified: HELD/approve-first, server-side TEST-MODE tenant gate, no-fabrication guards, redirect-first sends. skindesign allowlist currently = the operator's own inbox only.
- Shared-DB test hazard: some integration tests write to whatever `ENGINE_DATABASE_URL` points at (they wiped some demo drafts once during this session). CI now provisions its own schema (ci.yml initdb+bootstrap step). Locally, prefer a scratch DB for full suite runs.
- The 3 uploaded Keebs artworks are clearly-labelled TEST placards (VLM honestly found no tattoo tags on them). Replace with real artwork uploads via the Artists tab.

## 4. In flight RIGHT NOW
- **PR #124** (draft, branch `claude/supervisor-state-recovery-4mp2vg` → main). CI was driven green across 5 rounds of fixes; every class was a fresh-clone/lane-hygiene issue, never product code: (1) ruff sweep; (2) integration lane ran on a bare DB → ci.yml now applies initdb + bootstrap_db.py; (3) four modules wrote ENGINE_DATABASE_URL into os.environ at import, un-skipping every skipif-guarded PG module in the DB-free unit lane → resolve-from-env; (4) smoke-gold-set autouse fixture no-ops without a DB; (5) done-gate: seed-dependent tests (hardcoded dev-machine cust ids / never-committed 60-customer seed) made self-seeding + by-email; the nmh.2 distinct-goals test re-pinned to the nmh.11 one-pending-per-recipient DDL contract (SEMANTIC DECISION: nmh.11's phantom-duplicate fix supersedes both-goals-land; to re-target a recipient, resolve their pending draft first).
- Operator's standing orders: (a) **merge all PRs** once CI is green (main is branch-protected: 5 required checks incl. done-gate, enforce_admins on); (b) keep THIS handoff file updated; (c) operator will supply Meta page token + real artwork images "in a while". OPENAI + SMTP creds received 07-09 and installed in engine/.env.

## 4b. MERGE OUTCOME (2026-07-09 ~15:15 UTC)
- **PR #124 MERGED to main** (merge commit 1d912a3) after 5 green checks incl. done-gate. The merge-commit method carried all constituent branch tips into main's history, auto-resolving 14 open PRs.
- **#111 (wwy.5 gold prune) MERGED** separately.
- **#94 (65w15) + #76 (a9m9 fixtures) CLOSED as superseded** (content already on main / base branch never merged) — comments on each explain.
- **#107 (fr1.4 RLS hardening) is the ONE remaining open PR**: retargeted to main, conflicts need a real resolution pass + a non-superuser role for the local bring-up before it can land (comment on the PR has details).
- Meta user token received (7 perms incl. instagram_content_publish) and stored in engine/.env; graph.facebook.com is BLOCKED from this sandbox, so run `python3 scripts/meta_mint_page_token.py` ON THE OPERATOR'S MACHINE within ~1h of token mint to exchange + save the permanent PAGE token / IG business id. api.openai.com is likewise blocked here — voice mint verified only as far as the egress wall; test voice locally.

## 4c. FULL-DUPLEX SUPERVISOR + ONE-BUTTON SEND (2026-07-09 ~18:40 UTC, on PR #129 branch)
Operator's latest order: supervisor must be a stateful full-duplex ORCHESTRATOR (plans, peeks inside agents real-time, detects contradictions, interrupts/tweaks/orders agents mid-run, enforces plan conformance) + ONE button to send all eligible emails. Built and LIVE-VERIFIED:
- **`engine/studio/supervisor_control.py`** (new): `run_directives` ledger (DDL `infra/initdb/24-run-directives.sql`, in bootstrap). Directive kinds — CLOSED set, can only narrow/redirect, never widen delivery or lift gates: `pause, abort, set_angle, set_offer, skip_lead, guide_copy, redo_lead`. `apply_directives` consumes exactly-once, records each application as a role='supervisor' agent_run (visible in the live panel). `review_run_coherence` = deterministic contradiction rules (angle-cites-empty-research / unsubstantiated-offer / ignores-dominant-objection) + optional clamped LLM read. `check_plan_conformance` = plan-vs-actual rules (research-missing / analysis-missing / artwork-missing), fire once each.
- **Executor wiring** (`agui.py` `_execute_provided_leads_sync`): `_consume_directives()` at every loop boundary (angle/offer/goal mutate mid-run; skip/redo queues; pause blocks, abort stops); conformance auto-enforcement — a violation injects a "Supervisor correction:" into the draft goal AND records a `conformance:auto` supervisor step (the supervisor literally re-prompts the agent until work matches plan). Routes: `POST /studio/run/{id}/steer`, `GET /studio/run/{id}/directives`, `POST /studio/run/{id}/review`; chat tools `steer_run`/`review_run` so the operator can steer by talking to the supervisor.
- **LIVE PROOF** (run `team-camp_edf133b74b21-1c29ab3214e2`): mid-run `set_angle` + `guide_copy` issued while drafting; roles show two `supervisor` steps between lead 1 and lead 2; directives ledger both `applied`; the NEXT draft opens with the redirected angle ("Keebs just opened a short booking window… Weekday slots are moving fast"). Steering demonstrably changed subsequent agent output mid-run.
- **One-button send**: `POST /studio/send-eligible` (tenant-wide; `live` must be explicitly true) → `send_eligible` per-draft through the REAL `approve_and_publish` (exactly-once claim + TEST-MODE gate + allow-list/redirect). FIX: a gate-blocked draft (e.g. IG action, recipient None, skindesign test-mode) is now reported under `skipped` with the gate's reason instead of 500-ing the whole batch — regression test `test_send_eligible_gate_blocked_draft_is_reported_not_fatal`. Live safe-mode result on the real queue: `sent 0 / failed 0 / skipped 5` (3 emails = no jury confidence → approval required; 2 IG = TEST-MODE gate) — honest, nothing bypassed. UI: `SendAllEligible` button in ReviewScreen (confirm-on-live, per-bucket result line).
- Tests: 22/22 green in `test_campaign_send.py` + `test_supervisor_control.py`.
- **initech-pattern fleet (operator's explicit reference: github.com/nmelo/initech)** — `engine/studio/supervisor_fleet.py`: `fleet_status` (= `initech status`: per-run activity working/stalled/waiting-operator/done/failed from runs+agent_runs+actions+run_directives; awaiting-selection read from artwork_selections), `patrol_once` (= `initech patrol`: stall detection + deterministic-only coherence rules, zero tokens; each NEW finding recorded once as a role='supervisor' agent_run, deduped per run+rule), `start_patrol_loop` (background asyncio loop, `SUPERVISOR_PATROL_SECONDS` default 60, 0 disables). Routes `GET /studio/fleet`, `POST /studio/fleet/patrol`; chat tool `fleet_status`; console `FleetBoard` strip on the Runs tab (polls /studio/fleet, shows only in-flight runs). LIVE PROOF: seeded a stalled run → the 30s background tick found it on its own, logged `run stalled: no agent step for 922.5s (last role: draft)` and recorded the supervisor step (then cleaned up). `test_supervisor_fleet.py` 4/4 green. `review_run_coherence` gained `llm: bool = True` so patrol runs rules-only.

## 4d. PR #129 MERGED (2026-07-09 19:07 UTC)
- **PR #129 MERGED to main** (merge commit 39d3372) with all 5 checks green — carries everything in §4c (full-duplex supervisor, initech fleet/patrol, one-button send-all, artwork word-token fix, pack-loader test hygiene). Branch `claude/supervisor-state-recovery-4mp2vg` reset onto the new main and re-pushed (remote branch had been auto-deleted on merge). Session auto-unsubscribed from the PR. §26 acceptance re-verified live this session — all pass except two sandbox-only limits: research evidence links empty (proxy began 403-blocking Firecrawl mid-session; honest degradation, works off-sandbox) and actual email delivery (dead Gmail token + SMTP egress blocked here; works on the operator's machine).
- Only remaining open PR: **#107** (fr1.4 RLS hardening — needs a real conflict pass).

## 4e. REAL KEEBS PORTFOLIO + VIDEO PIPELINE + IG DEEP RESEARCH (2026-07-09 ~20:00 UTC)
- **CRITICAL: the Anthropic API key in engine/.env ran OUT OF CREDITS ~19:30 UTC** ("credit balance too low"). Live LLM drafting, jury, VLM, supervisor LLM reads all honestly degrade until the operator tops up. Deterministic paths (coherence rules, patrol, artwork selection, send gates) still work.
- **Keebs portfolio CORRECTED (operator's order)**: the 157 'natalie-flash-tattoos' images were GENERIC SHOP tattoos, not Keebs' personal work. All 157 retagged `collection='studio-generic'` / artist='Skin Design Studio' (assets + context_artifacts unlinked from keebs; 155 false keebs memories deleted). REAL portfolio arrived in 3 batches: 8 imgs (PR #131, branch `Cardano-max-patch-1`), 7 imgs + 1 mp4 (PR #132, branch `Cardano-max-patch-2`), 7 imgs shared inline in chat (no files — recorded as described memories pending file upload). 15 real images ingested from `.inbound/keebs-real/`; VLM declined (credits) so tags were written by THIS SESSION'S OWN VISION (recorded model 'session-vision (operator session, 2026-07-09)') — real analysis of the real images. Verified: theme ranking picks dragon→dragon, anime→Nezuko, botanical→woodland calf, marvel→Spider-Man sleeve. 24+ memories written incl. style profile (4 lanes: color realism/anime; b&g realism Marvel/oni/lions; Japanese neo-trad; botanical) + proven campaign voice (real 06.25 Keebs $1,200 campaign: scarcity hook, 'Text KEEBS' CTA, payment-plan objection handler). RE-INGEST AFTER CONTAINER LOSS: fetch branches `Cardano-max-patch-1/2`, extract via `git show`, run `scripts/ingest_artwork_dir.py .inbound/keebs-real Keebs`.
- **VIDEO pipeline (new)**: `engine/studio/video_ingest.py` — bytes → ≤5 evenly-spaced REAL frames (bundled static ffmpeg, new dep `imageio-ffmpeg`) → citation-gated VLM per frame → merged union tags with per-timestamp facts → artifact (new type 'video'; idempotent CHECK migration in 20-context-artifacts.sql) + b-roll library row (`content.media='video'`) + artist memory. `/studio/upload/image` dispatches `video/*`. UI: shared `MediaUpload` (image+video) on Memory, Agency, Voice tabs; Artists uploader accepts video. LIVE-PROVEN with the real Keebs mp4 (24.35s → 5 frames → art_vid_214314c173ef7641 + asset + memory; session-vision tags: Marvel-sleeve reveal b-roll reel).
- **IG deep research (new)**: `run_deep_social_research` = 3 cited-only angles (instagram-trends / reddit-community site:reddit.com / hooks-and-formats) + `load_brand_patterns` (proven hooks/CTAs from campaign_examples + brand_voice/style_profile memories) + `load_broll` — all in the IG brief; crew records `trend_research` + `hook_research` + `brand_patterns` steps. Drafter ordered to MOLD cited trends into proven brand hooks/CTAs, keywords only from cited sources, b-roll only from real library videos. Firecrawl currently 403-blocked by THIS sandbox (worked earlier today) — honest degradation; works on operator's machine. Test re-pinned: `test_ig_pipeline_depth` (4 crew steps, 3-angle header).
- **Send→memory awareness (spec: "memory knows who we sent to, what, which image")**: `send_eligible` now writes an artist memory row per SENT draft naming an artist (target, subject, artwork asset id, run id, mode).
- PRs #131/#132 (image uploads): images extracted + ingested; close PRs, KEEP BRANCHES as the asset archive (do not delete).
- **Operator's acceptance scenario (stated ~19:55, run when credits are topped up)**: supervisor task → Keebs customers who stepped back on price/time → per-customer psychometrics + socials + local events/trends (football fans get football angle etc.), separate strategy per customer → mid-run top-4 artwork pause matched to customer likes → one-click send-eligible → Keebs memory shows who/what/which image → new design upload updates DB+memory → ask supervisor where a new b-roll fits (channel reasoning).

## 4f. LOCAL BRING-UP + VOICE/SESSIONS (2026-07-09 ~22:45 UTC — all MERGED to main)
- **PR #133 MERGED** (44466d3): `scripts/run-local.ps1` — one-command Windows bring-up (docker check with hard timeout, adopts the operator's existing `scalers-postgres` container to keep old data, initdb via container psql, engine :8000 + console :3005, `-IngestKeebs` flag). Operator's machine debugging: Docker Desktop WSL2 engine flaky (npipe missing) — fix sequence: quit Docker Desktop → `wsl --shutdown` → reopen as admin.
- **PR #134 MERGED** (d475a2e): (a) VOICE GROUNDING — the voice host was narrating fabricated progress ("strategist has delivered" with 0 agents landed); ORCHESTRATE now narrates only steps present in the run-state briefing, GO-GATE reports refused/failed launches verbatim (launch real only with a spoken run id). (b) ONE CONVERSATION voice+text: `POST /studio/voice/turn` persists spoken lines to `studio_chat_turns`; the voice mint injects a CONVERSATION-SO-FAR briefing for the session; FE joins spoken lines into the typed host's thread. (c) CHAT-STYLE SESSIONS: `GET /studio/sessions` + `GET /studio/sessions/{id}/turns`; stateful sessionId in StudioRunProvider (localStorage) with switchSession/newSession; SessionSwitcher on the Voice surface. Model-policy guard gotcha: the literal string "Claude-style" in engine source trips `test_no_model_id_above_the_policy_in_production_source`.
- **Operator's LOCAL DB gotchas found live**: legacy Ladies-First fixture rows inside skindesign (pre-r8-gate seeding) — cleanup SQL given (deactivate tenant_documents/context_artifacts ILIKE '%ladies%', delete fixture memories/customers); local DB lacks cloud-only history (today's Keebs winback memories) — operator bootstrapped via a staged run for Carlos Hernandez/Shaquille Sledge/Isabella Johnston; local campaign_examples are for ANGEL (use Angel for proven-voice demo acts, Keebs for portfolio/video acts). Demo one-click live-send proof: allowlist the operator's own inbox + `cust_demo_ateeb` customer (SQL given in chat) — real customers stay gated.

## 4g. TWO-ENGINES BUILD BURST (2026-07-10, PR #136 branch) — conversation intake, scheduler, executive brain, 3-agent integration
Operator's orders this window: exact-words conversation CSV + ingest; TWO-ENGINES plan (docs/PLAN-TWO-ENGINES.md — Social Growth w/ competitor creative intelligence, Warm Lead Reactivation; operator's own safety correction adopted: NO sensitive-trait inference, evidence-based personalization only); "make everything live and fully verified". Built + LIVE-VERIFIED on the real DB:
- **Conversation-CSV intake** (`studio/conversation_import.py`): speaker+text header detection, verbatim turns → `lead_conversations`, customers upserted, explicit stop/unsubscribe → `sms_opt_in=false` (opt-out travels with the data). Deliverable `keebs-conversations.csv` (128 verbatim messages from the 5 real threads) sent to operator. Amanda's "Stop" auto-captured on upload.
- **Approve-and-schedule spine** (`studio/scheduler.py` + 27-action-schedule.sql + store columns): schedule = approval gesture w/ timestamp on ONE pending draft; background sweep publishes due rows through the REAL `approve_and_publish` (all gates intact); gate refusal CLEARS the schedule (no silent retry). Chat tool `schedule_draft` is approval-gated. `ACTION_SCHEDULER_SECONDS` loop (60s).
- **Campaign Intelligence executive brain** (`studio/intelligence.py`, GET /studio/intelligence, `campaign_intelligence_brief` chat tool, IntelligencePanel on Overview): deterministic aggregation over the five memories; every recommendation carries evidence. Live: 84 price-objection leads; Natalie 183/235 delivery cited; now also the competitor-mold rec.
- **3 parallel builder agents integrated** (worktrees, 3-way patches; one agui.py upload-chain conflict resolved as competitor→conversation→appointment→customers):
  1. **Appointments/performance import** (`studio/appointment_import.py`, 25-appointments.sql, upload branch, 5/5 tests): both real exports ingested LIVE — a_1.csv: 16 customers/17 appts/26 sessions/$19,633 deposits; a_2.csv: 81 customers/106 appts/146 sessions with 26 cross-file duplicates caught by the natural key. Per-customer history memories dossier-visible. Live DB: 146 session rows, $146,688 deposits.
  2. **Competitor creative intelligence** (`studio/competitor_intel.py`, 26-competitor-posts.sql, upload branch, ig_pipeline MOLD block + `competitor_intel` crew step, 14 tests): deterministic 0–10 scoring w/ honest no-data exclusion + renormalization; niche/style match via word-token overlap against OUR real library; deconstruction (hook/angle/CTA verbatim) + never-copy mold orders. Live: 2 demo posts scored 7.32/7.05; brain recommends molding the top one.
  3. **Objection intelligence** (reason_history/psych_profile/customer_research/skill_select, 12 tests): new taxonomy `trust_concern`/`blocked_by_prereq`/`went_quiet_mid_booking` w/ customer-vs-studio evidence source (stated vs inferred, never asserted as their words); angles `rebuild-trust` (offer WITHHELD)/`prereq-help` (no discount)/`resume-booking` (references the exact stalled step); SMS suppression guard downgrades even an explicitly requested SMS when `sms_opt_in=false`. LIVE on the real threads: Amanda→trust_concern (her verbatim refund turn), Todd→went_quiet_mid_booking (unanswered photo ask), Lauren→blocked_by_prereq (laser-removal turn); all 3 sms-suppressed.
- **Keebs IG handle**: operator gave https://www.instagram.com/keebs_sdt; direct fetch is 403-blocked server-side, so the handle + honest fetch status is on artist memory (`mem_54db3bc80807496b`); full panel analysis awaits the Meta page token or an operator posts-CSV export.
- Real PII stays out of git: a_1/a_2 CSVs live in session uploads / `.inbound/` only.

## 5. Next steps (in order)
1. DONE — #124 merged; see §4b.
2. After merge: re-point the local stack at main, re-run `scripts/run-local.sh` sanity.
3. When the operator provides real artwork images → upload via Artists tab → verify VLM tags are real (style/motif/color) → re-run the artwork-pause scenario with real tags matching (e.g. "lion linework" query should rank the lion piece first).
4. When Meta page token with perms arrives → set in `engine/.env` → test IG publish to the studio's own account (app-tester scope), using `PUBLIC_ASSET_BASE_URL` or tunnel for media.
5. Real email delivery proof on the operator's machine (SMTP) or after Gmail re-consent (works from anywhere incl. this sandbox since it's HTTPS).
6. Voice supervisor: OPENAI_API_KEY is now set — mint `/studio/voice/session` and test the speaking flow (may need OpenAI credits if 429).
7. Remaining spec items not yet built: Facebook-specific pipeline variant (§11 FB tone), Drive/Instagram artwork import (blocked here; buttons are honest stubs), campaign outcome/performance feedback loop (§5 "what worked"), 500-lead durable cohort executor (tlv.1 branch — major conflicts, deferred), reply/outcome capture memory (tlv.2 — failed QA earlier, deferred).

## 6. Key artifacts
- `docs/CREDENTIALS-AND-EXTERNALS.md` — verified credential statuses + how-to-get.
- `scripts/run-local.sh`, `engine/bootstrap_db.py` — bring-up.
- `.inbound/` (gitignored) — real PII CSVs + campaign transcription JSON.
- Screenshots of the finished UI: scratchpad `final-shots/` (artists-roster, keebs-profile, review-queue).
- PR #124: https://github.com/Cardano-max/Scalers/pull/124
