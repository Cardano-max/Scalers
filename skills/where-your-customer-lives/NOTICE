where-your-customer-lives skill
===============================

This skill is a PATTERN-ONLY re-authoring. It reproduces NONE of any upstream
skill's text, scripts, or assets — only the high-level idea ("from an ICP, find
the communities customers are in + per-channel entry tactics"), retargeted to
tattoo-native communities and re-written as original methodology grounded in
docs/skills/winning-strategies-kb.md.

Upstream pattern : "where-your-customer-lives" (r/ClaudeAI 20-skills list;
                   family: coreyhaines31/marketingskills, MIT)
Pinned commit    : 8bfcdffb655f16e713940cd04fb08891899c47db   # sec resolves/verifies the real 40-hex SHA
                   at fetch; non-loadable until filled (vetting-protocol.md).

STRIPPED (never vendored)
-------------------------
- `fetch.py` — disabled TLS verification (`ssl._create_unverified_context()` /
  `CERT_NONE`). Removed in full; fetching now runs through the vetted research
  adapter with TLS verification restored.
- `.env.example` + `GITHUB_TOKEN` read — removed; no credential harvesting.
- The 67 bundled live-API CLIs from the parent marketingskills repo — not vendored.

WHAT SURVIVES
-------------
SKILL.md (original methodology) + references/*.md only. Prompt-only; no network,
file, exec, or credential surface. Network is the research adapter's job.
