# Engine dev/test toolchain (test/CI scaffold, dhv.5).
# Mirrors [dependency-groups].dev in pyproject.toml for pip-based CI installs.
# Runtime deps (langgraph, pydantic-ai, fastapi, ...) come from `pip install -e .`.
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27          # FastAPI TestClient transport
pytest-cov>=5.0
ruff>=0.6
# `ruff format` is Black-compatible, so Black is not required. Uncomment if a
# team member prefers the Black CLI directly — config lives in pyproject.toml.
# black>=24.0
